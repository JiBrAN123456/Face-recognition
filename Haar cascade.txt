download the required files to make the haar cascade file.Collect all the posituve and negative images.Mark the area you need in our case the faces of people using the object marker tool that we got. Open the create list file in the negative images folder and list will be created . Count the number of files in positive and negative images you have in the folder and put the number in the haar training file. Run the haar training file. Then run the convert file and our haar file will be created. Create a virtual environment wherever you wanna use it.
install opencv import your haarcascade file and use it using the face cascade function and it will detect the faces